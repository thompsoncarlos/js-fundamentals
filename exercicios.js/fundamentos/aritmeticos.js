const [a, b, c, d] = [3, 5, 1, 15]; // using destructiring operator on the left side.

const soma = a + b + c + d; // 24 // operando in fix (operador entre dois valores)
const subtracao = d - b; // 10
const multiplicacao = a * b; // 15
const divisao = d / a; // 5
const modulo = a % 2; // 1
// d++ // operando e operador(++) pós fix (adiciona apos)
// -a // operando unário

console.log(soma, subtracao, multiplicacao, divisao, modulo);
