const number1 = 50;
const number2 = 43;
const number3 = 12;

// Using Array
// with values in array is called element
const nums = [50, 43, 12];
console.log(nums);

// Array can stored anyone data type: string, number, boolean etc.
const array = [50, "Banana", true];
console.log(array);

// each element position called index
// index          0    1   2   3
const elements = [50, 43, 12, 48];
console.log(elements[1]); 

// Method length is used to know the size of array
console.log(elements.length);
