// concat method don't change original array. Return a new array containing all arrays or values passed as a param. 

var roomPython = ['Eduardo', 'Matheus', 'Miguel'];
var roomJS = ['Liana', 'Fábio', 'Carlos'];
var allRoom = roomPython.concat(roomJS);
console.log(allRoom);