// Challenge:
// Print the index and the list with numbers: 100, 200, 300, 400, 500, 600.

const numbers = [100, 200, 300, 400, 500, 600];

for (let i = 0; i < numbers.length; i++) {
    console.log(i, numbers[i]);
}